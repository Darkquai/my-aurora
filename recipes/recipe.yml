# yaml-language-server: $schema=schema.blue-build.org
name: my-aurora
description: This is my personal OS image.

base-image: ghcr.io/ublue-os/aurora-dx-nvidia-open
image-version: latest

modules:
  - type: files
    files:
      - source: system
        destination: /

  - type: dnf
    repos:
      copr:
        - atim/starship
    install:
      packages:
        - micro
        - starship
        - pv
        - virt-manager
        - gparted
        - "@virtualization"
    remove:
      packages:
        - firefox
        - firefox-langpacks

  - type: default-flatpaks
    configurations:
      - notify: true
        scope: system
        install:
          - org.mozilla.firefox
          - org.gnome.Loupe
          - com.jeffser.Alpaca
          - org.blender.Blender
          - com.bambulab.BambuStudio
          - org.freecadweb.FreeCAD
          - org.gimp.GIMP
          - org.videolan.VLC
          - org.jupyter.JupyterLab
          - io.github.shiftey.Desktop
          - org.openscad.OpenSCAD
          - com.wings3d.WINGS
          # DX Apps
          - com.visualstudio.code
          - io.podman_desktop.PodmanDesktop
          - com.raggesilver.BlackBox
          - org.gnome.Boxes
          - com.github.tchx84.Flatseal
          - org.gnome.Ptyxis
      - scope: user
        install: [] # Added empty list to satisfy the schema requirements

  - type: homebrew
    # Taps must be listed here as 'user/repo'
    taps:
      - charmbracelet/tap
      - gptscript-ai/tap
      - blockprotocol/tap
    # Packages should be listed here by name only
    packages:
      - uv
      - ripgrep
      - bat
      - eza
      - fzf
      - zoxide
      - walk
      - syft
      - yq
      - aichat
      - block-goose-cli
      - gemini-cli
      - mods
      - ramalama
      - llm
      - opencode
      - qwen-code
      - whisper-cpp
      # Fully qualified names or casks can remain as they are expected formats:
      - gptscript-ai/tap/clio
      - charmbracelet/tap/crush
      - --cask codex

  - type: systemd
    system:
      enabled:
        - docker.service
        - podman.socket
        - libvirtd.service

  - type: signing
